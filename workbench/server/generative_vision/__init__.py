"""generative_vision — perception + symbolic (Prolog) recognizers for the
workbench's Video Import / Recognition pipeline.

Currently imported in-tree; intended to become a standalone installable package
(``pip install``) later. Keep submodules dependency-light and path-relative.
"""
