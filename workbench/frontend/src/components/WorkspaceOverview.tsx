type Summary={workflows:number;goals:number;operations:number;datatypes:number;representations:number;models:number;files:number};
export function WorkspaceOverview({label,summary,onOpenWorkflow}:{label:string;summary:Summary;onOpenWorkflow:()=>void}){
 const rows=[["Goals",summary.goals,"workspace intent"],["Operations",summary.operations,"available capabilities"],["Datatypes",summary.datatypes+summary.representations,"typed contracts"],["Models",summary.models,"model choices"],["Workspace files",summary.files,"persistent knowledge"]] as const;
 return <section className="resource-view workspace-overview"><div className="resource-heading"><div><span>HUMAN + AI BLACKBOARD</span><h1>{label}</h1><p>A shared workspace for defining goals, assembling Operations into workflows, working with knowledge, and learning how tools cooperate.</p></div><button onClick={onOpenWorkflow}>Open current workflow</button></div><div className="checks-summary"><div className="check-score">{summary.workflows}<small> workflows</small><span>PLANNED WORK</span></div><div className="check-list">{rows.map(([name,count,role])=><div key={name}><span>•</span><b>{name}</b><small>{count} filesystem-backed items</small><em>{role}</em></div>)}</div></div></section>;
}
