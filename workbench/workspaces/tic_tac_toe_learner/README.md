# Tic-Tac-Toe Learner

A deliberately small filesystem workspace for learning the workbench.

The workflow accepts a board observation, preserves it as an artifact, waits for a human move, and returns both the observed board and selected move. It demonstrates real workflow loading, typed inputs, a backend task, a human-input step, artifacts, events, and outputs.
