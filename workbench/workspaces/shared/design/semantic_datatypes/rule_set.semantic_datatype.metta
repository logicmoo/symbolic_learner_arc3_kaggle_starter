(
  (kind semantic_datatype)
  (id rule_set)
  (label "Rule Set")
  (description "A semantic collection of rules independent of the language or executable representation used to encode them.")
  (parents ([]
    information
  ))
)
