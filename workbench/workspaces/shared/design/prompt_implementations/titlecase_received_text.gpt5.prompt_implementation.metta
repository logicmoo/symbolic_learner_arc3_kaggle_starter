({}
  (kind prompt_implementation)
  (id titlecase_received_text.gpt5)
  (label "Titlecase Received Text — GPT-5")
  (version 1)
  (targets (
    openai
    gpt-5
    gpt-5.6
  ))
  (text (
    "Transform the supplied text to title case."
    "Do not rewrite, summarize, explain, or add content."
    "Preserve punctuation and spacing except where capitalization requires a change."
    "Output only the transformed text."
  ))
  (parents (
    titlecase_received_text
  ))
)
