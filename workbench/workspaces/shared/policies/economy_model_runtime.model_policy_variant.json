{
  "kind": "model_policy_variant",
  "id": "economy_model_runtime",
  "implements": "default_model_runtime",
  "label": "Economy Runtime",
  "description": "Prefer low-cost healthy models and retain premium models only as fallbacks.",
  "enabled": false,
  "example": true,
  "selection": {
    "strategy": "weighted",
    "fallback": true,
    "weights": {"quality": 0.2, "latency": 0.2, "cost": 0.5, "reliability": 0.1}
  },
  "defaults": {"wanted": "auto", "runtime": "auto", "benchmark": "off"}
}
