(
  (kind operation)
  (categories ([] collection/random))
  (id collection.random_list_element)
  (label "Random List Element")
  (description "Select one actual element from a non-empty list using an optional deterministic seed.")
  (inputs ((items Array) (seed Number)))
  (outputs ((element Any)))
  (children ([] collection.random_list_element.python))
  (preferredChild collection.random_list_element.python)
)
(
  (kind operation)
  (categories ([] collection/random sample/python))
  (id collection.random_list_element.python)
  (label "Random List Element / Python")
  (implementation python.callable)
  (inputs ((items Array) (seed Number)))
  (outputs ((element Any)))
  (python ((importMode file) (file python/collection_operations.py) (callable random_list_element)))
  (parameters ((outputBinding element)))
  (parents ([] collection.random_list_element))
)
