(
  (kind concrete_datatype)
  (id turtle_source)
  (label "Turtle Source")
  (implements (
    (turtle_program ())
  ))
  (mimeTypes ([]
    text/x-logo
    text/plain
  ))
  (extensions ([]
    .logo
    .txt
  ))
  (inheritsFrom (
    (turtle_program (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
