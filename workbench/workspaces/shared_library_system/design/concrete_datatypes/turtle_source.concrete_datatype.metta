(
  (kind concrete_datatype)
  (id turtle_source)
  (label "Turtle Source")
  (implements (
    (turtle_program (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    text/x-logo
    text/plain
  ))
  (extensions ([]
    .logo
    .txt
  ))
)
