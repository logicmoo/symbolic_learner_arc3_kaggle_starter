(
  (kind concrete_datatype)
  (id png)
  (label Png)
  (implements (
    (bitmap (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    image/png
  ))
  (extensions ([]
    .png
  ))
)
