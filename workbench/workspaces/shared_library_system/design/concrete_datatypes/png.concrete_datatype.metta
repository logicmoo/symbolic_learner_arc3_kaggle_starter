(
  (kind concrete_datatype)
  (id png)
  (label Png)
  (implements (
    (bitmap ())
  ))
  (mimeTypes ([]
    image/png
  ))
  (extensions ([]
    .png
  ))
  (inheritsFrom (
    (bitmap (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
