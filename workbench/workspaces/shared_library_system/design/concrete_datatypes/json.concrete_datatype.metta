(
  (kind concrete_datatype)
  (id json)
  (label Json)
  (implements (
    (json_object ())
    (object_list ())
    (scene_graph ())
  ))
  (mimeTypes ([]
    application/json
  ))
  (extensions ([]
    .json
  ))
  (inheritsFrom (
    (json_object (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
    (object_list (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
    (scene_graph (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
