(
  (kind concrete_datatype)
  (id text_plain)
  (label "Text Plain")
  (implements (
    (natural_language (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    text/plain
  ))
  (extensions ([]
    .txt
  ))
)
