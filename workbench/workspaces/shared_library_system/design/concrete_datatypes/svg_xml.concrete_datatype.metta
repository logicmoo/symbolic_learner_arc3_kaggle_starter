(
  (kind concrete_datatype)
  (id svg_xml)
  (label "Svg Xml")
  (implements (
    (svg (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    image/svg+xml
  ))
  (extensions ([]
    .svg
  ))
)
