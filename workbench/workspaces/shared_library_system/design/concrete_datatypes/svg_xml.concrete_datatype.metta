(
  (kind concrete_datatype)
  (id svg_xml)
  (label "Svg Xml")
  (implements (
    (svg ())
  ))
  (mimeTypes ([]
    image/svg+xml
  ))
  (extensions ([]
    .svg
  ))
  (inheritsFrom (
    (svg (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
