(
  (kind concrete_datatype)
  (id bmp)
  (label Bmp)
  (implements (
    (bitmap (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    image/bmp
  ))
  (extensions ([]
    .bmp
  ))
)
