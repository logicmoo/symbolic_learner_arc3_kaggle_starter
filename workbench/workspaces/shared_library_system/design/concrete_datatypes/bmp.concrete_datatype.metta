(
  (kind concrete_datatype)
  (id bmp)
  (label Bmp)
  (implements (
    (bitmap ())
  ))
  (mimeTypes ([]
    image/bmp
  ))
  (extensions ([]
    .bmp
  ))
  (inheritsFrom (
    (bitmap (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
