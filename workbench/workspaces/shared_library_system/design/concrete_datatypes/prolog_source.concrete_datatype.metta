(
  (kind concrete_datatype)
  (id prolog_source)
  (label "Prolog Source")
  (implements (
    (prolog_fact_set ())
  ))
  (mimeTypes ([]
    text/x-prolog
    text/plain
  ))
  (extensions ([]
    .pl
  ))
  (inheritsFrom (
    (prolog_fact_set (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
