(
  (kind concrete_datatype)
  (id prolog_source)
  (label "Prolog Source")
  (implements (
    (prolog_fact_set (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    text/x-prolog
    text/plain
  ))
  (extensions ([]
    .pl
  ))
)
