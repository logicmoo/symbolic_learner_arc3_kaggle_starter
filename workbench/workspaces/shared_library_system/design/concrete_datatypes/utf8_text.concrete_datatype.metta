(
  (kind concrete_datatype)
  (id utf8_text)
  (label "Utf8 Text")
  (implements (
    (plain_text ())
  ))
  (mimeTypes ([]
    text/plain
  ))
  (extensions ([]
    .txt
  ))
  (inheritsFrom (
    (plain_text (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
