(
  (kind concrete_datatype)
  (id utf8_text)
  (label "Utf8 Text")
  (implements (
    (plain_text (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    text/plain
  ))
  (extensions ([]
    .txt
  ))
)
