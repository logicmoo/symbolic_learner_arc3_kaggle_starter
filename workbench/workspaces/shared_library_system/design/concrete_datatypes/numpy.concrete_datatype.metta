(
  (kind concrete_datatype)
  (id numpy)
  (label Numpy)
  (implements (
    (latent_embedding (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    application/x-npy
  ))
  (extensions ([]
    .npy
  ))
)
