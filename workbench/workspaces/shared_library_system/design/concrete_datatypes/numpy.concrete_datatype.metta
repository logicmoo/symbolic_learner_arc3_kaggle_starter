(
  (kind concrete_datatype)
  (id numpy)
  (label Numpy)
  (implements (
    (latent_embedding ())
  ))
  (mimeTypes ([]
    application/x-npy
  ))
  (extensions ([]
    .npy
  ))
  (inheritsFrom (
    (latent_embedding (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
