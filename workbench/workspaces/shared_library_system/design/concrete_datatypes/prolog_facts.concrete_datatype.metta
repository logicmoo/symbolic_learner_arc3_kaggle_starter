(
  (kind concrete_datatype)
  (id prolog_facts)
  (label "Prolog Facts")
  (implements (
    (object_list ())
  ))
  (mimeTypes ([]
    text/x-prolog
    text/plain
  ))
  (extensions ([]
    .pl
  ))
  (inheritsFrom (
    (object_list (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
