(
  (kind concrete_datatype)
  (id prolog_facts)
  (label "Prolog Facts")
  (implements (
    (object_list (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    text/x-prolog
    text/plain
  ))
  (extensions ([]
    .pl
  ))
)
