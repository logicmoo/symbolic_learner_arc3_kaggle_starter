(
  (kind concrete_datatype)
  (id markdown)
  (label Markdown)
  (implements (
    (natural_language ())
    (plain_text ())
  ))
  (mimeTypes ([]
    text/markdown
  ))
  (extensions ([]
    .md
  ))
  (inheritsFrom (
    (natural_language (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
    (plain_text (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
