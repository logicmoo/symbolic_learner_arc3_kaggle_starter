(
  (kind concrete_datatype)
  (id markdown)
  (label Markdown)
  (implements (
    (natural_language (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
    (plain_text (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    text/markdown
  ))
  (extensions ([]
    .md
  ))
)
