(
  (kind concrete_datatype)
  (id vector_json)
  (label "Vector Json")
  (implements (
    (latent_embedding ())
  ))
  (mimeTypes ([]
    application/json
  ))
  (extensions ([]
    .json
  ))
  (inheritsFrom (
    (latent_embedding (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
