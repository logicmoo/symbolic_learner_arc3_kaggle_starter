(
  (kind concrete_datatype)
  (id vector_json)
  (label "Vector Json")
  (implements (
    (latent_embedding (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (mimeTypes ([]
    application/json
  ))
  (extensions ([]
    .json
  ))
)
