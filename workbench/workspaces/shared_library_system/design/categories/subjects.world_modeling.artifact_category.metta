(
  (kind artifact_category)
  (id subjects.world_modeling)
  (label "World Modeling")
  (description "Operations that build or revise a world model: entities, transitions, hypotheses, and evidence that describe how a world behaves.")
  (path subjects/world-modeling)
  (trees ([]
    operations
  ))
  (query (
    (kinds ([]
      operation
    ))
    (where (
      (subjects (
        (contains world-modeling)
      ))
    ))
  ))
  (parentMode show)
)
