(
  (kind semantic_datatype)
  (id object_representation_bundle)
  (label "Object Representation Bundle")
  (description "A semantic bundle tying multiple synchronized representations to the same entity or object set.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
