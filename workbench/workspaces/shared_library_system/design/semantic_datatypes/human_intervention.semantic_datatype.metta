(
  (kind semantic_datatype)
  (id human_intervention)
  (label "Human Intervention")
  (implements (
    (intervention (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
  (description "An intervention chosen by a human demonstrator.")
)
