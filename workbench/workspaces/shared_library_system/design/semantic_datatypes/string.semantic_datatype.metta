(
  (kind semantic_datatype)
  (id string)
  (label String)
  (description "A sequence of characters used when an operation requires a scalar string rather than general textual content.")
  (parents ([]
    information
  ))
)
