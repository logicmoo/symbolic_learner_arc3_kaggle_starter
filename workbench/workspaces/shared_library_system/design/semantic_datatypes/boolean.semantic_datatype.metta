(
  (kind semantic_datatype)
  (id boolean)
  (label Boolean)
  (description "A truth value independent of its serialized or runtime representation.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
