(
  (kind semantic_datatype)
  (id atomspace_query_result)
  (label "AtomSpace Query Result")
  (description "Bindings and matching Atoms returned by an AtomSpace query.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
