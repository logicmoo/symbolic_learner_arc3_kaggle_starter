(
  (kind semantic_datatype)
  (id world_analysis_state)
  (label "World Analysis State")
  (description "The current semantic state of the workbench's accumulated world analysis.")
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
