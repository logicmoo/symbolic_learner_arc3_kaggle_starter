(
  (kind semantic_datatype)
  (id atomspace_change)
  (label "AtomSpace Change")
  (description "A structured assertion or retraction and the Event caused by changing an AtomSpace.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
