(
  (kind semantic_datatype)
  (id file_reference)
  (label "File Reference")
  (description "A durable reference to a filesystem or provider-backed resource, independent of the referenced file's content type.")
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
