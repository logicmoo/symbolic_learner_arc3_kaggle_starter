(
  (kind semantic_datatype)
  (id artifact_category)
  (label "Artifact Category")
  (description "A first-class query-backed category specification used to organize resource trees.")
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
