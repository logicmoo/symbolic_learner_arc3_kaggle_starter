(
  (kind semantic_datatype)
  (id category_match_set)
  (label "Category Match Set")
  (description "The resources selected by evaluating an Artifact Category query.")
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
