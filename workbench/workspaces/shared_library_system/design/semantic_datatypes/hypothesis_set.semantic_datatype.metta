(
  (kind semantic_datatype)
  (id hypothesis_set)
  (label "Hypothesis Set")
  (description "A collection of alternative or competing hypotheses considered together by a reasoning operation.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
