(
  (kind semantic_datatype)
  (id identity_map)
  (label "Identity Map")
  (description "Cross-view and cross-time identity assignments between entity observations.")
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
