(
  (kind semantic_datatype)
  (id goal_set)
  (label "Goal Set")
  (description "Supplied or inferred semantic goals used to guide simulation and evaluation.")
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
