(
  (kind semantic_datatype)
  (id system_contract)
  (label "System Contract")
  (description "A callable runtime, agent, MCP server, plugin, or communication-system specification.")
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
