(
  (kind semantic_datatype)
  (id atom)
  (label Atom)
  (description "A symbolic entity that exists in an AtomSpace and may denote a fact, value, relation, rule, or evidence item.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
