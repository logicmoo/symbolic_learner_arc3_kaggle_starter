(
  (kind semantic_datatype)
  (id datatype_sample)
  (label "Datatype Sample")
  (description "A representative value generated for a declared datatype contract.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
