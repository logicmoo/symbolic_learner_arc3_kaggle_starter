(
  (kind semantic_datatype)
  (id policy_decision)
  (label "Policy Decision")
  (description "An evaluated policy outcome together with its inheritance and explanatory inputs.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
