(
  (kind semantic_datatype)
  (id planning_strategy)
  (label "Planning Strategy")
  (description "A strategy that proposes, selects, or generates a Planned Workflow.")
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
