(
  (kind semantic_datatype)
  (id prompt_resource)
  (label "Prompt Resource")
  (description "A prompt specification or inherited prompt variant before rendering.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
