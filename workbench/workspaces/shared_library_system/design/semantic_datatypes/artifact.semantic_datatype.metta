(
  (kind semantic_datatype)
  (id artifact)
  (label Artifact)
  (description "A typed persistent output stored in a workspace Knowledge or Runtime artifact location.")
  (categories ([]
    knowledge/artifacts
  ))
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
