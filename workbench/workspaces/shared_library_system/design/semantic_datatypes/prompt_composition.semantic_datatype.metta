(
  (kind semantic_datatype)
  (id prompt_composition)
  (label "Prompt Composition")
  (description "An ordered composition of resolved prompt members and separators.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
