(
  (kind semantic_datatype)
  (id turtle_program_set)
  (label "Turtle Program Set")
  (description "A collection of executable Turtle drawing programs treated as one semantic program value.")
  (implements (
    (program ())
  ))
  (inheritsFrom (
    (program (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
