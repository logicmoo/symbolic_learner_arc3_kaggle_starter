(
  (kind semantic_datatype)
  (id resource_validation)
  (label "Resource Validation")
  (description "A structured validation result for a workbench resource.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
