(
  (kind semantic_datatype)
  (id goal)
  (label Goal)
  (description "A semantic desired outcome, success condition, or objective.")
  (implements (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
