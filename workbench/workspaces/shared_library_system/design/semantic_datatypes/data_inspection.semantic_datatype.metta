(
  (kind semantic_datatype)
  (id data_inspection)
  (label "Data Inspection")
  (description "Metadata describing the type, format, size, path, and loaded-value state of workspace Data.")
  (categories ([] knowledge/data inspection))
  (parents ([] information))
)
