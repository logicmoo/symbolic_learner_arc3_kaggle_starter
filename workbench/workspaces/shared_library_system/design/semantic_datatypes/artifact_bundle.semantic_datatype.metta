(
  (kind semantic_datatype)
  (id artifact_bundle)
  (label "Artifact Bundle")
  (description "A named aggregate of related workbench artifacts, their identities, and provenance references.")
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
