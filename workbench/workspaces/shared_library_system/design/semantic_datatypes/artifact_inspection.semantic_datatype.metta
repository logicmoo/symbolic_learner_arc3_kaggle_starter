(
  (kind semantic_datatype)
  (id artifact_inspection)
  (label "Artifact Inspection")
  (description "Metadata describing a persisted Artifact's type, media format, size, path, and storage lifecycle.")
  (categories ([]
    knowledge/artifacts
    inspection
  ))
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
