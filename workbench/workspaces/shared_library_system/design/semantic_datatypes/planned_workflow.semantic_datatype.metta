(
  (kind semantic_datatype)
  (id planned_workflow)
  (label "Planned Workflow")
  (description "An ordered or partially ordered workflow produced by planning.")
  (implements (
    (information ())
  ))
  (inheritsFrom (
    (information (
      (borrow ([]
        *
      ))
      (exclude ([]))
    ))
  ))
)
