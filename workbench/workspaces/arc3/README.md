# ARC3 Workspace

This workspace is loaded directly from the filesystem by the MeTTa Symbolic Learner Workbench.

Its workflow files are real engine documents. Running them creates persisted runs, step states, artifacts, events, logs, and human-input waits in the backend workflow engine.

The initial workflow demonstrates the generic observation/action cycle without hard-coding ARC3 behavior into the engine itself.
