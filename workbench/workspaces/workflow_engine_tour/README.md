# Workflow Engine Tour

A richer filesystem workspace demonstrating dependency scheduling, fan-out, fan-in, conditions, retries, artifacts, and event inspection.

This workspace is intentionally separate from the Tic-Tac-Toe Learner so users can choose either a minimal example or a broader engine example at startup.
